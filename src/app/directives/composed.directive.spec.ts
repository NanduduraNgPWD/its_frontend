import { ComposedDirective } from './composed.directive';

describe('ComposedDirective', () => {
  it('should create an instance', () => {
    const directive = new ComposedDirective();
    expect(directive).toBeTruthy();
  });
});
